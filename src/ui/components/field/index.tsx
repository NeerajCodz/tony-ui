/**
 * Field Component - Dynamic Renderer
 * Generated by script
 */

import React, { lazy, Suspense } from 'react';
// import type { FieldProps, FieldVersion } from '../../types/components/field.js';

// Fallback types if file doesn't exist
type FieldVersion = string;
interface FieldProps extends React.HTMLAttributes<HTMLElement> {
  version?: FieldVersion;
  variant?: 'neutral' | 'primary' | 'success' | 'warning' | 'destructive' | 'info';
  type?: 'default' | 'outline' | 'solid' | 'ghost';
  [key: string]: any;
}

// Config import
let componentConfig: any = { versions: {} };
try {
  // @ts-ignore
  // componentConfig = require('../../config/components/field.json');
} catch (e) {
  // console.warn('Config not found for field');
}

// Dynamic imports for versions
const versionComponents: Record<string, any> = {
  'angular-corner': lazy(() => import('./field-angular-corner.tsx')),
  'holo-frame': lazy(() => import('./field-holo-frame.tsx')),
  'data-panel': lazy(() => import('./field-data-panel.tsx')),
  'circuit-board': lazy(() => import('./field-circuit-board.tsx')),
  'quantum-gate': lazy(() => import('./field-quantum-gate.tsx')),
  'tactical-hud': lazy(() => import('./field-tactical-hud.tsx')),
  'energy-shield': lazy(() => import('./field-energy-shield.tsx')),
  'terminal-window': lazy(() => import('./field-terminal-window.tsx')),
  'matrix-grid': lazy(() => import('./field-matrix-grid.tsx')),
  'neon-outline': lazy(() => import('./field-neon-outline.tsx')),
};

// Loading Skeleton
const LoadingSkeleton: React.FC = () => (
  <div className="animate-pulse bg-muted/20 w-full h-full min-h-[40px] rounded" />
);

// Fallback Component
const FallbackComponent = React.forwardRef<HTMLElement, FieldProps>(({ 
  children, 
  className = '',
  version,
  ...props 
}, ref) => (
  <div ref={ref as any} className={`p-4 border border-dashed border-red-500/50 bg-red-500/10 ${className}`} {...props}>
    <span className="text-xs text-red-400 font-mono">Missing: {version}</span>
    {children}
  </div>
));
FallbackComponent.displayName = 'FallbackField';

// Subcomponents

// Main Component
const FieldBase = React.forwardRef<HTMLElement, FieldProps>(({ 
  version = 'angular-corner',
  ...props 
}, ref) => {
  const VersionComponent = versionComponents[version];
  
  if (!VersionComponent) {
    return <FallbackComponent ref={ref} version={version} {...props} />;
  }

  return (
    <Suspense fallback={<LoadingSkeleton />}>
      <VersionComponent ref={ref} {...props} />
    </Suspense>
  );
});
FieldBase.displayName = 'Field';

export const Field = FieldBase;

export default Field;

