'use client';

import { createIconButtonFoundation } from '../_shared/family-foundations';

const foundation = createIconButtonFoundation('large');

export const IconButton = foundation.IconButton;

export default IconButton;
