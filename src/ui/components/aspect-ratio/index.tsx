/**
 * AspectRatio Component - Dynamic Renderer
 * Generated by script
 */

import React, { lazy, Suspense } from 'react';
// import type { AspectRatioProps, AspectRatioVersion } from '../../types/components/aspect-ratio.js';

// Fallback types if file doesn't exist
type AspectRatioVersion = string;
interface AspectRatioProps extends React.HTMLAttributes<HTMLElement> {
  version?: AspectRatioVersion;
  variant?: 'neutral' | 'primary' | 'success' | 'warning' | 'destructive' | 'info';
  type?: 'default' | 'outline' | 'solid' | 'ghost';
  [key: string]: any;
}

// Config import
let componentConfig: any = { versions: {} };
try {
  // @ts-ignore
  // componentConfig = require('../../config/components/aspect-ratio.json');
} catch (e) {
  // console.warn('Config not found for aspect-ratio');
}

// Dynamic imports for versions
const versionComponents: Record<string, any> = {
  'angular-corner': lazy(() => import('./aspect-ratio-angular-corner.tsx')),
  'holo-frame': lazy(() => import('./aspect-ratio-holo-frame.tsx')),
  'data-panel': lazy(() => import('./aspect-ratio-data-panel.tsx')),
  'circuit-board': lazy(() => import('./aspect-ratio-circuit-board.tsx')),
  'quantum-gate': lazy(() => import('./aspect-ratio-quantum-gate.tsx')),
  'tactical-hud': lazy(() => import('./aspect-ratio-tactical-hud.tsx')),
  'energy-shield': lazy(() => import('./aspect-ratio-energy-shield.tsx')),
  'terminal-window': lazy(() => import('./aspect-ratio-terminal-window.tsx')),
  'matrix-grid': lazy(() => import('./aspect-ratio-matrix-grid.tsx')),
  'neon-outline': lazy(() => import('./aspect-ratio-neon-outline.tsx')),
};

// Loading Skeleton
const LoadingSkeleton: React.FC = () => (
  <div className="animate-pulse bg-muted/20 w-full h-full min-h-[40px] rounded" />
);

// Fallback Component
const FallbackComponent = React.forwardRef<HTMLElement, AspectRatioProps>(({ 
  children, 
  className = '',
  version,
  ...props 
}, ref) => (
  <div ref={ref as any} className={`p-4 border border-dashed border-red-500/50 bg-red-500/10 ${className}`} {...props}>
    <span className="text-xs text-red-400 font-mono">Missing: {version}</span>
    {children}
  </div>
));
FallbackComponent.displayName = 'FallbackAspectRatio';

// Subcomponents

// Main Component
const AspectRatioBase = React.forwardRef<HTMLElement, AspectRatioProps>(({ 
  version = 'angular-corner',
  ...props 
}, ref) => {
  const VersionComponent = versionComponents[version];
  
  if (!VersionComponent) {
    return <FallbackComponent ref={ref} version={version} {...props} />;
  }

  return (
    <Suspense fallback={<LoadingSkeleton />}>
      <VersionComponent ref={ref} {...props} />
    </Suspense>
  );
});
AspectRatioBase.displayName = 'AspectRatio';

export const AspectRatio = AspectRatioBase;

export default AspectRatio;

