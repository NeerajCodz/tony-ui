'use client';

import { createAnalogClockFoundation } from '../_shared/family-foundations';

const foundation = createAnalogClockFoundation('matrix-grid');

export const AnalogClock = foundation.AnalogClock;

export default AnalogClock;
