/**
 * Kbd Component - Dynamic Renderer
 * Generated by script
 */

import React, { lazy, Suspense } from 'react';
// import type { KbdProps, KbdVersion } from '../../types/components/kbd.js';

// Fallback types if file doesn't exist
type KbdVersion = string;
interface KbdProps extends React.HTMLAttributes<HTMLElement> {
  version?: KbdVersion;
  variant?: 'neutral' | 'primary' | 'success' | 'warning' | 'destructive' | 'info';
  type?: 'default' | 'outline' | 'solid' | 'ghost';
  [key: string]: any;
}

// Config import
let componentConfig: any = { versions: {} };
try {
  // @ts-ignore
  // componentConfig = require('../../config/components/kbd.json');
} catch (e) {
  // console.warn('Config not found for kbd');
}

// Dynamic imports for versions
const versionComponents: Record<string, any> = {
  'angular-corner': lazy(() => import('./kbd-angular-corner.tsx')),
  'holo-frame': lazy(() => import('./kbd-holo-frame.tsx')),
  'data-panel': lazy(() => import('./kbd-data-panel.tsx')),
  'circuit-board': lazy(() => import('./kbd-circuit-board.tsx')),
  'quantum-gate': lazy(() => import('./kbd-quantum-gate.tsx')),
  'tactical-hud': lazy(() => import('./kbd-tactical-hud.tsx')),
  'energy-shield': lazy(() => import('./kbd-energy-shield.tsx')),
  'terminal-window': lazy(() => import('./kbd-terminal-window.tsx')),
  'matrix-grid': lazy(() => import('./kbd-matrix-grid.tsx')),
  'neon-outline': lazy(() => import('./kbd-neon-outline.tsx')),
};

// Loading Skeleton
const LoadingSkeleton: React.FC = () => (
  <div className="animate-pulse bg-muted/20 w-full h-full min-h-[40px] rounded" />
);

// Fallback Component
const FallbackComponent = React.forwardRef<HTMLElement, KbdProps>(({ 
  children, 
  className = '',
  version,
  ...props 
}, ref) => (
  <div ref={ref as any} className={`p-4 border border-dashed border-red-500/50 bg-red-500/10 ${className}`} {...props}>
    <span className="text-xs text-red-400 font-mono">Missing: {version}</span>
    {children}
  </div>
));
FallbackComponent.displayName = 'FallbackKbd';

// Subcomponents

// Main Component
const KbdBase = React.forwardRef<HTMLElement, KbdProps>(({ 
  version = 'angular-corner',
  ...props 
}, ref) => {
  const VersionComponent = versionComponents[version];
  
  if (!VersionComponent) {
    return <FallbackComponent ref={ref} version={version} {...props} />;
  }

  return (
    <Suspense fallback={<LoadingSkeleton />}>
      <VersionComponent ref={ref} {...props} />
    </Suspense>
  );
});
KbdBase.displayName = 'Kbd';

export const Kbd = KbdBase;

export default Kbd;

