/**
 * ButtonGroup Component - Dynamic Renderer
 * Generated by script
 */

import React, { lazy, Suspense } from 'react';
// import type { ButtonGroupProps, ButtonGroupVersion } from '../../types/components/button-group.js';

// Fallback types if file doesn't exist
type ButtonGroupVersion = string;
interface ButtonGroupProps extends React.HTMLAttributes<HTMLElement> {
  version?: ButtonGroupVersion;
  variant?: 'neutral' | 'primary' | 'success' | 'warning' | 'destructive' | 'info';
  type?: 'default' | 'outline' | 'solid' | 'ghost';
  [key: string]: any;
}

// Config import
let componentConfig: any = { versions: {} };
try {
  // @ts-ignore
  // componentConfig = require('../../config/components/button-group.json');
} catch (e) {
  // console.warn('Config not found for button-group');
}

// Dynamic imports for versions
const versionComponents: Record<string, any> = {
  'angular-corner': lazy(() => import('./button-group-angular-corner.tsx')),
  'holo-frame': lazy(() => import('./button-group-holo-frame.tsx')),
  'data-panel': lazy(() => import('./button-group-data-panel.tsx')),
  'circuit-board': lazy(() => import('./button-group-circuit-board.tsx')),
  'quantum-gate': lazy(() => import('./button-group-quantum-gate.tsx')),
  'tactical-hud': lazy(() => import('./button-group-tactical-hud.tsx')),
  'energy-shield': lazy(() => import('./button-group-energy-shield.tsx')),
  'terminal-window': lazy(() => import('./button-group-terminal-window.tsx')),
  'matrix-grid': lazy(() => import('./button-group-matrix-grid.tsx')),
  'neon-outline': lazy(() => import('./button-group-neon-outline.tsx')),
};

// Loading Skeleton
const LoadingSkeleton: React.FC = () => (
  <div className="animate-pulse bg-muted/20 w-full h-full min-h-[40px] rounded" />
);

// Fallback Component
const FallbackComponent = React.forwardRef<HTMLElement, ButtonGroupProps>(({ 
  children, 
  className = '',
  version,
  ...props 
}, ref) => (
  <div ref={ref as any} className={`p-4 border border-dashed border-red-500/50 bg-red-500/10 ${className}`} {...props}>
    <span className="text-xs text-red-400 font-mono">Missing: {version}</span>
    {children}
  </div>
));
FallbackComponent.displayName = 'FallbackButtonGroup';

// Subcomponents

// Main Component
const ButtonGroupBase = React.forwardRef<HTMLElement, ButtonGroupProps>(({ 
  version = 'angular-corner',
  ...props 
}, ref) => {
  const VersionComponent = versionComponents[version];
  
  if (!VersionComponent) {
    return <FallbackComponent ref={ref} version={version} {...props} />;
  }

  return (
    <Suspense fallback={<LoadingSkeleton />}>
      <VersionComponent ref={ref} {...props} />
    </Suspense>
  );
});
ButtonGroupBase.displayName = 'ButtonGroup';

export const ButtonGroup = ButtonGroupBase;

export default ButtonGroup;

